import type { HighlightsSectionProps } from '../../types';

/** Studio uses hero + testimonials for highlights; keep slot empty. */
export default function StudioHighlightsSection(_props: HighlightsSectionProps) {
  return null;
}
