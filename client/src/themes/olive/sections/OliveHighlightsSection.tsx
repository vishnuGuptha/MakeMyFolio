import type { HighlightsSectionProps } from '../../types';

/** Stats live in About for Olive; keep Highlights empty to avoid duplicate strips. */
export default function OliveHighlightsSection(_props: HighlightsSectionProps) {
  return null;
}
